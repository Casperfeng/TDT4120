'use strict';
//Not implemented yet
function radixSort(A, d) {
    for (let i = 0; i < d; i++) {
        // use a stable sort to sort array A on digit i
        // stable sorts: insertion, bubble, bucket, merge, counting etc.
    }
}