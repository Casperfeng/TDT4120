'use strict';
//Not implemented yet

//chapter 8 section 3 - Radix sort
//best case Ω(nk)
//avg case Θ(nk)
//worst case O(nk)

/**
 * sorts by comparing digits, d
 * @param A is the array to be sorted
 * @param d is the number of digits
 */
function radixSort(A, d) {
    for (let i = 0; i < d; i++) {
        // use a stable sort to sort array A on digit i
        // stable sorts: insertion, bubble, bucket, merge, counting etc.
    }
}