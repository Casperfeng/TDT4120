'use strict';

//chapter 2 section 3.1 - The divide-and-conquer approach

/**
 * helper function for mergeSort
 * divides an array into 2 smaller sub arrays L and R
 *
 * best case Ω(n log(n))
 * avg case Θ(n log(n))
 * worst case O(n log(n))
 *
 * @param A is the main array to be sorted
 * @param p is an index
 * @param q is an index
 * @param r is an index
 * given that p <= q <= r
 */

function merge(A, p, q, r){

}


function mergeSort(){

}